name: DITA Validation
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - name: Validate XML (Platzhalter)
        run: |
          echo "Validierungsskript wird hier eingetragen ^(AP 3^)"
